page.replace_html "all_report_items", :partial => "report_item", :collection => @report_items
page['report_item_form'].attr("action", url_for(:action => 'display_report_items', :start_time => (@start_time - 5.hours).to_s, :only_path => true) )
