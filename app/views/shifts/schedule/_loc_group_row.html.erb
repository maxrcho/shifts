<% loc_group_preprocessing %>

<%= render :partial => "shifts/schedule/location_row_set", :collection => Location.active.in_group(loc_group_row).sort_by(&:short_name).reverse.sort_by(&:priority).reverse, :locals => {:day => day} %>
<%if @has_shifts%>
	<ul class="events divider" style="height: <%= @divider_height %>px;"></ul>
<%else%>
	<div class="toggle-empty" style="display:none">
		<ul class="events divider" style="height: <%= @divider_height %>px;"></ul>
	</div>
<%end%>
<% @location_group_row += 1 %>
